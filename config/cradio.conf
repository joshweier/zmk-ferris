
# Put configuration options here
CONFIG_ZMK_IDLE_TIMEOUT=30000
CONFIG_ZMK_SLEEP=y

# ZMK Studio: live keymap editing over USB. Re-locks after 5 min idle;
# press &studio_unlock (sys layer) to make edits.
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCK_IDLE_TIMEOUT_SEC=300
